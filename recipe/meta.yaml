package:
  name: iotaa
  version: 0.7.1 # reset build number
source:
  path: ../src
build:
  include_recipe: false
  noarch: python
  number: 1
requirements:
  build:
    - pip
  host:
    - python
test:
  requires:
    - black
    - coverage
    - docformatter
    - isort
    - mypy
    - pylint
    - pytest
about:
  home: https://github.com/maddenp/iotaa
  license: APACHE
